# Assets

Screenshots and visual assets for the README.

## Needed

Add the following screenshots once real output is available:

| File | What to capture |
|------|----------------|
| `match-gap-table.png` | The match/gap analysis table output in Claude Code after JD analysis |
| `generated-resume-example.png` | A section of a generated DOCX (use a fake/anonymised example) |
| `application-tracker.png` | Terminal output of the tracker `list` command |
| `batch-run.png` | Claude Code output after a batch run |

## Guidelines

- Remove or blur all personal information (name, phone, email, address) before committing
- Use real tool output where possible — no mocked screenshots
- PNG format, 1200–1600px wide

Once added, reference them in README.md:

```markdown
![Match Gap Table](assets/match-gap-table.png)
```
